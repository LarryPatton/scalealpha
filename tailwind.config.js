/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      /* ============================================
       * 自定义响应式断点 - 多分辨率屏幕适配
       * 默认断点: sm(640px), md(768px), lg(1024px), xl(1280px), 2xl(1536px)
       * 新增断点: 3xl(1920px), 4xl(2560px), 5xl(3840px)
       * ============================================ */
      screens: {
        '3xl': '1920px',  // 1K FHD 全高清
        '4xl': '2560px',  // 2K QHD 
        '5xl': '3840px',  // 4K UHD
      },
      colors: {
        dark: '#1a1a1a',
        'dark-secondary': '#2a2a2a',
      },
      /* ============================================
       * 响应式间距扩展 - 用于大屏幕适配
       * ============================================ */
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '100': '25rem',
        '112': '28rem',
        '128': '32rem',
      },
      /* ============================================
       * 响应式最大宽度扩展
       * ============================================ */
      maxWidth: {
        '8xl': '88rem',   // 1408px
        '9xl': '96rem',   // 1536px
        '10xl': '120rem', // 1920px
        'screen-3xl': '1920px',
        'screen-4xl': '2560px',
        'screen-5xl': '3840px',
      },
      /* ============================================
       * 圆角优化 - 从工业硬朗风格改为现代柔和风格
       * 原始值: sm=2px, DEFAULT=4px, md=6px, lg=8px
       * 新值:   sm=4px, DEFAULT=6px, md=8px, lg=12px
       * ============================================ */
      borderRadius: {
        'sm': '4px',      // 原 2px → 4px (小按钮、标签、Badge)
        DEFAULT: '6px',   // 原 4px → 6px (输入框、普通按钮)
        'md': '8px',      // 原 6px → 8px (小卡片、面板)
        'lg': '12px',     // 原 8px → 12px (卡片、模态框)
        'xl': '16px',     // 原 12px → 16px (大卡片)
        '2xl': '20px',    // 原 16px → 20px (超大卡片)
      },
    },
  },
  plugins: [],
}
