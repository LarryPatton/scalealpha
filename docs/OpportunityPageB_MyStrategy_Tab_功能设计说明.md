# OpportunityPageB - 我的策略 Tab 功能设计说明

> **页面路径**: `http://localhost:5176/scalealpha/opportunityB?tab=mystrategy`  
> **组件文件**: `src/pages/OpportunityPageB.vue`  
> **Tab 参数**: `?tab=mystrategy` (默认 Tab)  
> **最后更新**: 2026-01-20

---

## 1. 概述

「我的策略」Tab 是 OpportunityPageB 的核心工作区，用户在此管理所有已生成的投资策略、查看策略详情、生成执行计划并执行交易。该 Tab 采用三栏式布局，提供完整的策略生命周期管理功能。

---

## 2. 页面布局

### 2.1 整体结构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              Navbar                                      │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌──────────── Zone A: 活动任务区域 ─────────────────────────────────┐   │
│ │  [处理中任务卡片]  [待处理任务卡片]  [待处理任务卡片]  [+N更多]     │   │
│ └────────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│ ┌───────────┐ ┌────────────────────────────────────────────────────┐    │
│ │ 侧边栏    │ │                 策略列表表格                        │    │
│ │           │ │                                                    │    │
│ │ 智能筛选  │ │  ┌─────────────────────────────────────────────┐  │    │
│ │ ───────── │ │  │ 表头: 选择 | Ticker | 策略 | 执行计划 | 操作 │  │    │
│ │ 分组管理  │ │  ├─────────────────────────────────────────────┤  │    │
│ │ ───────── │ │  │ 策略行 1                                    │  │    │
│ │ [新建分组]│ │  │   └── 展开的执行计划子表格                  │  │    │
│ │           │ │  │ 策略行 2                                    │  │    │
│ │           │ │  │ 策略行 3                                    │  │    │
│ │           │ │  │ ...                                         │  │    │
│ └───────────┘ └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 尺寸规范

| 区域 | 宽度 | 特性 |
|------|------|------|
| 侧边栏 | 224px (w-56) | 固定宽度 |
| 策略列表 | flex-1 | 自适应剩余空间 |
| 活动任务区域 | 100% | 全宽，可折叠 |

---

## 3. Zone A: 活动任务区域

### 3.1 功能描述

显示当前正在生成和等待生成的策略任务，让用户实时了解任务进度。

### 3.2 任务状态

| 状态 | 标识 | 显示样式 |
|------|------|---------|
| `pending` | 等待中 | 灰色卡片，显示预计时间 |
| `processing` | 处理中 | 青色边框，显示进度条和剩余时间 |

### 3.3 任务卡片结构

**处理中任务卡片** (280px 宽):
```
┌──────────────────────────────────┐
│ AAPL    [处理中]    [已选中]  2:30│
│ ████████████░░░░░░░░  68%        │
│ Analyzing market sentiment...    │
└──────────────────────────────────┘
```

**待处理任务卡片** (200px 宽):
```
┌────────────────────────┐
│ GOOGL         [等待中] │
│ 预计: ~3分钟           │
└────────────────────────┘
```

### 3.4 交互功能

- **点击任务卡片**: 选中该任务，右侧显示策略预览
- **展开/折叠**: 当任务数超过可见数量时，可展开查看全部
- **+N 更多卡片**: 点击展开所有隐藏任务

### 3.5 并发控制

```javascript
const maxConcurrent = ref(2) // 最多同时处理 2 个任务
```

---

## 4. 策略侧边栏

### 4.1 智能筛选器 (Smart Filters)

| 筛选项 | 筛选逻辑 | 图标颜色 |
|-------|----------|---------|
| 高评级 (A/B) | `grade === 'A' \|\| grade === 'B'` | 青色 |
| 建议更新 | `strategyNeedsUpdate === true` | 橙色 |
| 无信号记录 | 无明确交易信号的策略 | 灰色 |
| 未发现机会 | `grade === 'N/A'` | 红色 |

**启用/禁用开关**: 可通过切换开关一键禁用所有智能筛选。

### 4.2 分组管理 (Collections)

#### 4.2.1 系统默认分组

| 分组 ID | 名称 | 筛选逻辑 |
|--------|------|---------|
| `all` | 全部策略 | 显示所有策略 |
| `official` | 官方策略 | `source === 'Official'` |
| `my` | 我的策略 | `source === 'My Strategy'` |
| `plans` | 有计划的 | `hasExecutionPlan === true` |
| `only-plan` | 仅计划 | `hasExecutionPlan && !hasStrategy` |

#### 4.2.2 自定义分组

- 点击「新建分组」创建自定义分组
- 分组名称可编辑
- 可删除自定义分组（策略不会被删除）
- 策略可移动到不同分组

### 4.3 分组数量统计

每个分组旁边显示该分组内的策略数量：
```javascript
strategyCollections = computed(() => [
  { id: 'all', label: '全部策略', count: allSavedStrategies.value.length, icon: IconList },
  { id: 'official', label: '官方策略', count: officialStrategiesCount.value, icon: IconVerified },
  // ...
])
```

---

## 5. 策略列表表格

### 5.1 表头结构

表头分为两层：分组表头 + 子列表头。

**分组表头**:
| 区域 | 说明 |
|------|------|
| 选择 | 全选复选框 |
| Ticker | 股票代码 |
| 策略 | 策略相关信息 (7列) |
| 执行计划 | 计划相关信息 (可展开, 1-5列) |
| 操作 | 操作按钮 |

**策略子列**:
| 列名 | 字段 | 宽度 |
|------|------|------|
| 名称 | stockName | 170-400px |
| 来源 | source | 80px |
| 方向 | direction | 80px |
| 评级 | grade | 55px |
| 期限 | horizon | 75px |
| 创建 | generatedAt | 90px |
| 更新 | strategyNeedsUpdate | 80px |

**执行计划子列** (可展开):
| 列名 | 字段 | 宽度 |
|------|------|------|
| 状态 | hasExecutionPlan | 85px |
| 数量 | planCount | 60px |
| 未读 | planUnreadCount | 60px |
| 生成中 | planGeneratingCount | 100px |
| 更新 | planNeedsUpdate | 85px |

### 5.2 列表排序

支持点击表头进行排序：
```javascript
handleSort(field) {
  if (strategySortField === field) {
    // 切换排序方向
    strategySortDirection = strategySortDirection === 'asc' ? 'desc' : 'asc'
  } else {
    strategySortField = field
    strategySortDirection = 'asc'
  }
}
```

### 5.3 策略状态显示

#### 5.3.1 评级显示

| 评级 | 样式 |
|------|------|
| A | 绿色圆角框 |
| B | 蓝色圆角框 |
| C | 琥珀色圆角框 |
| N/A | 灰色，整行半透明+灰度 |
| ERROR | 红色警告图标 + "失败" |

#### 5.3.2 方向显示

| 方向 | 样式 |
|------|------|
| LONG | 绿色标签 "LONG" |
| SHORT | 红色标签 "SHORT" |

#### 5.3.3 来源显示

| 来源 | 样式 |
|------|------|
| AI | 青色标签 |
| Manual | 橙色标签 |
| Import | 紫色标签 |
| Official | 特殊显示 |

#### 5.3.4 特殊状态

**生成失败 (ERROR)**:
- 整行显示半透明
- 评级列显示红色警告图标 + "失败"
- 操作列显示「重试」按钮

**已失效 (isExpired)**:
- 整行显示灰度效果
- 评级列显示灰色时钟图标 + "已失效"
- 生成计划按钮禁用

**正在更新 (regenerating)**:
- 整行覆盖蒙版
- 显示加载动画和进度条
- 禁止其他操作

### 5.4 策略行操作

| 操作 | 条件 | 图标 |
|------|------|------|
| 生成计划 | grade !== 'N/A' && !isCreditsInsufficient | ⚡ (绿色) |
| 查看计划 | hasExecutionPlan | 👁 (青色) |
| 删除 | 始终可用 | 🗑 (红色) |
| 重试 | hasError \|\| grade === 'ERROR' | 🔄 (黄色) |

### 5.5 工具栏功能

**左侧**:
- 当前分组名称
- 选中策略标签
- 策略数量统计

**右侧**:
- 移动到分组下拉菜单
- 搜索输入框
- 方向筛选下拉框

---

## 6. 执行计划子表格

### 6.1 展开条件

点击策略行的「查看计划」按钮展开。

### 6.2 子表格结构

```
┌──────────────────────────────────────────────────────────────────────┐
│ ⚡ 执行计划  [3]                                           [收起 ▲] │
├──────────────────────────────────────────────────────────────────────┤
│ 计划名称 │ 操作建议 │ 置信度 │ 入场价 │ 目标价 │ 止损价 │ 持仓 │ 创建时间 │ 操作 │
├──────────────────────────────────────────────────────────────────────┤
│ 激进增长计划 │ WAIT_BUY │ High │ $298.50 │ $317.50 │ $294.30 │ 15% │ 2025-12-17 │ 👁 🏦 📊 🗑 │
│ 保守收益计划 │ BUY │ Medium │ $298.50 │ $325.00 │ $294.30 │ -- │ 2025-12-18 │ 👁 🏦 📊 🗑 │
└──────────────────────────────────────────────────────────────────────┘
```

### 6.3 计划属性

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | String | 计划名称（可双击编辑） |
| `recommendation` | String | 操作建议 (BUY/SELL/HOLD/WAIT_BUY) |
| `confidence_level` | String | 置信度 (High/Medium/Low) |
| `entry_price` | Number | 入场价格 |
| `target_price` | Number | 目标价格 |
| `stop_loss_price` | Number | 止损价格 |
| `position_pct` | Number | 建议仓位百分比 |
| `createdAt` | String | 创建时间 |
| `isGenerating` | Boolean | 是否正在生成 |
| `generatingProgress` | Number | 生成进度 (0-100) |

### 6.4 计划操作

| 操作 | 按钮样式 | 功能 |
|------|---------|------|
| 查看详情 | 灰色边框 | 打开计划详情模态框 |
| 券商执行 | 绿色 | 执行到真实券商账户 |
| 模拟盘执行 | 青色 | 执行到模拟盘 |
| 删除 | 红色边框 | 删除该计划 |

---

## 7. 模态框系统

### 7.1 策略详情模态框

**触发**: 点击策略行的「查看」按钮

**布局** (95vw × 85vh):
```
┌─────────────────────────────────────────────────────────────────┐
│                                                          [✕]   │
├──────────────────┬─────────────────────────────┬────────────────┤
│    AI 对话区     │       策略内容区             │  关联计划区   │
│    (320px)       │       (flex-1)              │  (280px)      │
│                  │                             │               │
│ [快捷问题]       │ 投资论点                    │ ⚡ 执行计划    │
│                  │ 技术分析                    │               │
│ [对话历史]       │ 风险因素                    │ [计划卡片1]   │
│                  │                             │ [计划卡片2]   │
│ [输入框]         │ [查看股票详情] [分享]       │ [生成新计划]  │
└──────────────────┴─────────────────────────────┴────────────────┘
```

**AI 对话功能**:
- 快捷问题模板
- 实时对话
- 流式响应
- 积分消耗提示

### 7.2 生成计划模态框

**触发**: 点击「生成计划」按钮

**配置项** (820px 宽):
```
┌─────────────────────────────────────────────────────────────────┐
│  ⚡ 执行计划 | 配置参数                                  [✕]   │
├─────────────────────────────────────────────────────────────────┤
│  AAPL   Apple Inc.                      LONG   Grade A   $185.50│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐   ┌─────────────────────┐             │
│  │ Position Status     │   │ Preferences         │             │
│  │                     │   │                     │             │
│  │ [No Position]       │   │ Horizon:            │             │
│  │ [Have Position]     │   │ [Short][Medium][Long]│            │
│  │                     │   │                     │             │
│  │ Cost: $___          │   │ Risk Level: [====]  │             │
│  │ Size: ___%          │   │ Conservative        │             │
│  │                     │   │                     │             │
│  │ P&L: +$7.00 (+3.9%) │   │ □ Save as default   │             │
│  └─────────────────────┘   └─────────────────────┘             │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [NEW] [MEDIUM] [MODERATE]              [Cancel] [⚡ Generate] │
└─────────────────────────────────────────────────────────────────┘
```

**配置参数**:
| 参数 | 选项 | 默认值 |
|------|------|--------|
| 持仓状态 | 无持仓 / 有持仓 | 无持仓 |
| 成本价 | 数字输入 | - |
| 仓位占比 | 1-100% | - |
| 投资期限 | Short / Medium / Long | Medium |
| 风险偏好 | 0-100 滑块 | 50 |
| 保存为默认 | 复选框 | false |

### 7.3 执行计划详情模态框

**触发**: 点击计划的「查看详情」按钮

**内容结构**:
- Trading Plan (交易计划)
  - Current Action (当前操作建议)
  - Action Plan (入场计划)
  - Profit Taking Plan (止盈计划)
  - Stop Loss Plan (止损计划)
  - LLM Consensus (AI 模型共识)
- Plan Analysis (计划分析)
  - Analytical Approach
  - Primary Thesis vs Counter Thesis
  - Key Scenarios and Likelihoods
  - Primary Risks to Monitor
  - Manager's Note
  - Options/Cross Market Analysis
  - Broad Market Analysis

### 7.4 执行交易模态框

**触发**: 点击「券商执行」或「模拟盘执行」按钮

**配置项**:
| 参数 | 说明 |
|------|------|
| 账户类型 | 模拟盘 / 真实券商 |
| 账户选择 | 下拉选择具体账户 |
| 交易方向 | 买入 / 卖出 |
| 数量 | 股数 |
| 订单类型 | 市价单 / 限价单 |
| 价格 | 限价单价格 |
| 目标价 | 可选 |
| 止损价 | 可选 |

### 7.5 删除确认模态框

**触发**: 点击「删除」按钮

**内容**:
- 警告图标
- 策略信息摘要
- 关联计划数量提示
- 确认/取消按钮

---

## 8. 数据结构

### 8.1 策略对象

```typescript
interface Strategy {
  id: string
  symbol: string              // 股票代码
  stockName: string           // 股票名称
  direction: 'LONG' | 'SHORT' // 方向
  grade: 'A+' | 'A' | 'B' | 'C' | 'N/A' | 'ERROR' // 评级
  horizon: string             // 投资期限
  source: 'AI' | 'Manual' | 'Import' | 'Official' | 'My Strategy'
  generatedAt: string         // 生成时间
  savedAt: string             // 保存时间
  
  // 策略状态
  hasStrategy: boolean        // 是否有策略报告
  strategyNeedsUpdate: boolean // 是否建议更新
  strategyUnread: boolean     // 是否未读
  isExpired: boolean          // 是否已失效
  expiredReason?: string      // 失效原因
  hasError: boolean           // 是否生成失败
  errorMessage?: string       // 错误信息
  
  // 执行计划状态
  hasExecutionPlan: boolean   // 是否有执行计划
  planCount: number           // 计划数量
  planUnreadCount: number     // 未读计划数量
  planGeneratingCount: number // 正在生成的计划数量
  planNeedsUpdate: boolean    // 计划是否需要更新
  generatingCurrent?: number  // 当前生成进度项
  generatingProgress?: number // 生成进度百分比
  
  // Official 来源特有
  officialUpdating?: boolean  // 官方正在更新
  officialUpdated?: boolean   // 官方已更新
  
  // 分组
  categoryId?: string
  categoryName?: string
  isWatchlist?: boolean
  isArchived?: boolean
}
```

### 8.2 执行计划对象

```typescript
interface ExecutionPlan {
  id: number
  name: string                // 计划名称
  recommendation: 'BUY' | 'SELL' | 'HOLD' | 'WAIT_BUY' | 'WAIT_SELL'
  confidence_level: 'High' | 'Medium' | 'Low'
  entry_price: number         // 入场价
  target_price: number        // 目标价
  stop_loss_price: number     // 止损价
  position_pct: number | null // 建议仓位
  instruction: string         // 操作说明
  createdAt: string           // 创建时间
  
  // 生成状态
  isEditing: boolean          // 是否正在编辑名称
  isNew: boolean              // 是否新生成
  isGenerating: boolean       // 是否正在生成
  generatingProgress: number  // 生成进度
  isExpanded: boolean         // 是否展开详情
}
```

### 8.3 任务对象

```typescript
interface Task {
  id: number
  symbol: string
  stockName?: string
  title?: string
  status: 'pending' | 'processing' | 'completed'
  progress: number            // 0-100
  statusText: string          // 状态描述文本
  timeLeft: string            // 剩余时间
  estTime?: string            // 预计时间
  completedAt?: string        // 完成时间
  foundOpportunity?: boolean  // 是否发现机会
  type?: 'strategy' | 'plan'  // 任务类型
}
```

---

## 9. 状态管理

### 9.1 核心状态

```javascript
// 策略数据
const allSavedStrategies = ref([])      // 所有已保存策略
const selectedStrategyId = ref(null)     // 当前选中策略 ID
const selectedStrategy = ref({})         // 当前选中策略详情

// 任务数据
const tasks = ref([])                    // 所有任务
const pendingTasks = computed(...)       // 等待中的任务
const processingTasks = computed(...)    // 处理中的任务

// UI 状态
const selectedCollection = ref('all')    // 当前分组
const libraryFilter = ref('all')         // 智能筛选
const smartFiltersEnabled = ref(true)    // 智能筛选开关

// 表格状态
const strategySortField = ref('generatedAt')
const strategySortDirection = ref('desc')
const strategySearchQuery = ref('')
const strategyFilterOption = ref('all')  // all/long/short
const isExecutionPlanExpanded = ref(true) // 执行计划列展开

// 展开状态
const expandedStrategyIds = ref(new Set()) // 展开的策略ID
const isActiveGenExpanded = ref(false)     // 活动任务区展开

// 正在更新的策略
const regeneratingStrategies = ref({})    // { strategyId: { progress: number } }

// 计划缓存
const strategyPlansCache = ref(new Map()) // strategyId -> plans[]
```

### 9.2 计算属性

```javascript
// 当前显示的策略列表
const displayedStrategies = computed(() => {
  let result = allSavedStrategies.value
  
  // 1. 分组筛选
  if (selectedCollection !== 'all') {
    result = result.filter(filterByCollection)
  }
  
  // 2. 智能筛选
  if (smartFiltersEnabled && libraryFilter !== 'all') {
    result = result.filter(filterBySmartFilter)
  }
  
  // 3. 方向筛选
  if (strategyFilterOption !== 'all') {
    result = result.filter(s => s.direction.toLowerCase() === strategyFilterOption)
  }
  
  // 4. 搜索筛选
  if (strategySearchQuery) {
    result = result.filter(searchFilter)
  }
  
  // 5. 排序
  result = sortStrategies(result)
  
  return result
})

// 可见的处理中任务
const visibleProcessingTasks = computed(() => 
  processingTasks.value.slice(0, maxVisibleTasks.value)
)

// 隐藏的任务数量
const hiddenTasksCount = computed(() => {
  const total = pendingTasks.value.length + processingTasks.value.length
  return Math.max(0, total - maxVisibleTasks.value)
})
```

---

## 10. 交互流程

### 10.1 选择策略流程

```
用户点击策略行
       ↓
toggleStrategySelection(strategyId)
       ↓
更新 selectedStrategyId
       ↓
加载策略详情到 selectedStrategy
       ↓
右侧详情面板更新
```

### 10.2 生成执行计划流程

```
用户点击「生成计划」按钮
       ↓
打开 GeneratePlanModal
       ↓
用户配置参数 (持仓、期限、风险)
       ↓
点击「Generate」确认
       ↓
confirmGeneratePlan()
       ↓
创建新计划对象 (isGenerating: true)
       ↓
添加到 relatedPlans
       ↓
模拟生成进度更新
       ↓
完成后 isGenerating = false
       ↓
显示成功 Toast
```

### 10.3 执行交易流程

```
用户点击执行按钮
       ↓
openExecuteModal(plan, strategy, accountType, accountId)
       ↓
填写订单参数
       ↓
handleExecuteConfirm(orderData)
       ↓
判断账户类型
       ├── 模拟盘: addOrderToPortfolio()
       └── 券商: 添加到 broker_accounts
       ↓
显示成功 Toast
       ↓
关闭模态框
```

---

## 11. 本地存储

| Key | 描述 | 数据结构 |
|-----|------|---------|
| `saved_reports` | 已保存的策略报告 | Strategy[] |
| `plan_default_settings_v2` | 计划生成默认配置 | { investmentHorizon, riskPreference } |
| `broker_accounts` | 券商账户信息 | BrokerAccount[] |
| `paper_portfolios` | 模拟盘组合数据 | PaperPortfolio[] |

---

## 12. 积分系统

### 12.1 消耗规则

| 操作 | 积分消耗 |
|------|---------|
| 生成策略 | 20 积分 |
| 生成执行计划 | 10 积分 |
| AI 对话 (每次) | 2 积分 |

### 12.2 积分不足处理

- 生成计划按钮显示禁用状态
- AI 对话输入框显示禁用提示
- 显示「积分不足」警告条
- 提供跳转「充值」链接

---

## 13. 样式规范

### 13.1 颜色使用

| 元素 | 暗色模式 | 亮色模式 |
|------|---------|---------|
| 选中策略行 | accent.primary + '33' | #f0f9ff |
| 选中边框 | accent.primary | accent.primary |
| 悬停行 | background.elevated | #ffffff |
| 表格边框 | border.subtle | rgba(0,0,0,0.03) |

### 13.2 交互效果

| 效果 | 类名/样式 |
|------|---------|
| 选中发光 | glow-primary-md |
| 渐变进度条 | bg-gradient-to-r from-cyan-500 to-emerald-500 |
| 悬停上浮 | hover:-translate-y-px hover:shadow |
| 淡入动画 | animate-fade-in |
| 列表过渡 | TransitionGroup name="list" |

---

## 14. 响应式设计

### 14.1 断点适配

| 断点 | 调整 |
|------|------|
| < 1024px | 隐藏 AI 对话侧边栏 |
| < 768px | 执行计划列默认折叠 |

### 14.2 表格自适应

- 名称列宽度随执行计划列展开/折叠动态调整
- 折叠时: 400px
- 展开时: 170px

---

## 15. 性能优化

### 15.1 虚拟滚动

当策略数量过多时，考虑实现虚拟滚动。

### 15.2 计划缓存

```javascript
const strategyPlansCache = ref(new Map())

const getStrategyPlans = (strategy) => {
  if (strategyPlansCache.value.has(strategy.id)) {
    return strategyPlansCache.value.get(strategy.id)
  }
  // 加载计划...
  strategyPlansCache.value.set(strategy.id, plans)
  return plans
}
```

### 15.3 延迟加载

只在用户展开策略时才加载其执行计划。

---

## 附录

### A. 键盘快捷键 (规划中)

| 快捷键 | 功能 |
|--------|------|
| ↑/↓ | 选择上/下一个策略 |
| Enter | 打开策略详情 |
| Delete | 删除选中策略 |
| Esc | 关闭模态框 |

### B. 相关组件

- `ExecutionPlanDetailModal.vue` - 执行计划详情模态框
- `ExecutePlanModal.vue` - 执行交易模态框
- `Navbar.vue` - 导航栏

### C. 相关 Composables

- `useSavedReports` - 策略报告管理
- `usePaperPortfolio` - 模拟盘组合
- `useBrokerAccounts` - 券商账户管理
- `useDesignTokens` - 设计令牌

---

*文档版本: 1.0*  
*编写日期: 2026-01-20*
