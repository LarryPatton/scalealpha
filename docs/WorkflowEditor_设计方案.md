# 节点式策略工作流编辑器 设计方案

> **项目名称**: Strategy Workflow Editor (策略工作流编辑器)  
> **参考产品**: n8n, UE Blueprints, Node-RED, ComfyUI  
> **版本**: Draft v0.1  
> **日期**: 2026-01-20

---

## 一、设计目标

### 1.1 核心价值

将原本固定的「策略生成 → 计划生成 → 交易执行」流程，转变为**用户可自定义、可视化编排**的节点工作流，让用户能够：

1. **可视化理解**：直观看到策略生成的数据流和处理过程
2. **自定义流程**：按需组合不同的分析模块
3. **保存复用**：将自定义工作流保存为模板
4. **分享协作**：工作流可导出/导入/分享

### 1.2 目标用户场景

```
┌─────────────────────────────────────────────────────────────────┐
│ 场景 1: 普通用户                                                │
│   使用预置工作流模板，快速生成策略                              │
├─────────────────────────────────────────────────────────────────┤
│ 场景 2: 进阶用户                                                │
│   修改现有工作流，调整分析框架组合                              │
├─────────────────────────────────────────────────────────────────┤
│ 场景 3: 专业用户                                                │
│   从零构建自定义工作流，实现复杂策略逻辑                        │
├─────────────────────────────────────────────────────────────────┤
│ 场景 4: 团队协作                                                │
│   分享工作流给团队成员，统一策略生成标准                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、整体架构

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ▣ Strategy Workflow Editor                    [保存] [运行] [分享]  ⚙️     │
├──────────────┬──────────────────────────────────────────────┬───────────────┤
│              │                                              │               │
│  节点面板     │              画布区域 (Canvas)               │  属性面板     │
│  (Node       │                                              │  (Properties) │
│   Palette)   │   ┌──────┐      ┌──────┐      ┌──────┐      │               │
│              │   │Stock │ ───▶ │ AI   │ ───▶ │Plan  │      │  [选中节点]   │
│  ┌────────┐  │   │Select│      │Analyze│     │Gen   │      │               │
│  │📊 输入  │  │   └──────┘      └──────┘      └──────┘      │  参数配置     │
│  ├────────┤  │                                              │  ─────────    │
│  │⚙️ 分析  │  │                      │                      │  框架: [  ]   │
│  ├────────┤  │                      ▼                      │  期限: [  ]   │
│  │📋 策略  │  │               ┌──────────┐                  │  风险: [  ]   │
│  ├────────┤  │               │ Execute  │                  │               │
│  │⚡ 执行  │  │               │ Trade    │                  │               │
│  ├────────┤  │               └──────────┘                  │               │
│  │🔧 工具  │  │                                              │               │
│  └────────┘  │                                              │               │
│              │                                              │               │
├──────────────┴──────────────────────────────────────────────┴───────────────┤
│  [▶ 运行] [⏸ 暂停] [⏹ 停止]     进度: ████████░░ 80%     日志 | 结果       │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件

| 组件 | 功能 | 尺寸 |
|------|------|------|
| **节点面板** | 展示所有可用节点类型，支持拖拽 | 200-240px 宽 |
| **画布区域** | 放置节点、建立连接、执行预览 | flex-1 |
| **属性面板** | 配置选中节点的参数 | 280-320px 宽 |
| **底部控制栏** | 运行控制、进度显示、日志查看 | 48-60px 高 |

---

## 三、节点体系设计

### 3.1 节点分类

```
┌─────────────────────────────────────────────────────────────────┐
│                        节点分类体系                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   📊 输入节点 (Input)                                           │
│   ├── Stock Selector    - 股票选择器                           │
│   ├── Watchlist         - 关注列表                             │
│   ├── Portfolio Input   - 持仓输入                             │
│   └── Manual Ticker     - 手动输入代码                         │
│                                                                 │
│   ⚙️ 分析节点 (Analysis)                                        │
│   ├── Technical Analysis   - 技术分析                          │
│   ├── Fundamental Analysis - 基本面分析                        │
│   ├── Sentiment Analysis   - 情绪分析                          │
│   ├── Event Analysis       - 事件驱动分析                      │
│   ├── Quant Analysis       - 量化分析                          │
│   ├── Macro Analysis       - 宏观分析                          │
│   ├── Options Analysis     - 期权分析                          │
│   └── AI Auto Analysis     - AI 智能分析                       │
│                                                                 │
│   📋 策略节点 (Strategy)                                        │
│   ├── Strategy Generator   - 策略生成器                        │
│   ├── Strategy Merger      - 策略合并器                        │
│   ├── Strategy Filter      - 策略过滤器                        │
│   └── Grade Evaluator      - 评级评估器                        │
│                                                                 │
│   ⚡ 执行节点 (Execution)                                        │
│   ├── Plan Generator       - 计划生成器                        │
│   ├── Order Builder        - 订单构建器                        │
│   ├── Broker Executor      - 券商执行器                        │
│   └── Paper Trade          - 模拟盘交易                        │
│                                                                 │
│   🔧 工具节点 (Utility)                                         │
│   ├── Condition Switch     - 条件分支                          │
│   ├── Loop Iterator        - 循环迭代器                        │
│   ├── Data Transformer     - 数据转换器                        │
│   ├── Notification         - 通知节点                          │
│   ├── AI Chat              - AI 对话                           │
│   └── Save Output          - 保存输出                          │
│                                                                 │
│   🎯 特殊节点 (Special)                                         │
│   ├── Start                - 开始节点 (入口)                   │
│   ├── End                  - 结束节点 (出口)                   │
│   └── Comment              - 注释节点                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 节点数据结构

```typescript
// 节点基础定义
interface WorkflowNode {
  id: string                    // 唯一标识
  type: NodeType                // 节点类型
  position: { x: number, y: number }  // 画布位置
  data: NodeData                // 节点配置数据
  inputs: Port[]                // 输入端口
  outputs: Port[]               // 输出端口
  status: NodeStatus            // 运行状态
}

// 端口定义
interface Port {
  id: string
  name: string
  type: DataType                // 数据类型
  connected: boolean
  multiple: boolean             // 是否支持多连接
}

// 数据类型枚举
enum DataType {
  STOCK = 'stock',              // 单个股票
  STOCK_LIST = 'stock_list',    // 股票列表
  ANALYSIS = 'analysis',        // 分析结果
  STRATEGY = 'strategy',        // 策略
  STRATEGY_LIST = 'strategy_list',
  PLAN = 'plan',                // 执行计划
  ORDER = 'order',              // 订单
  BOOLEAN = 'boolean',          // 布尔值
  NUMBER = 'number',            // 数值
  STRING = 'string',            // 字符串
  ANY = 'any'                   // 任意类型
}

// 连接线定义
interface Edge {
  id: string
  source: string                // 源节点 ID
  sourcePort: string            // 源端口 ID
  target: string                // 目标节点 ID
  targetPort: string            // 目标端口 ID
  animated: boolean             // 是否动画
}
```

### 3.3 核心节点详细设计

#### 3.3.1 Stock Selector (股票选择器)

```
┌─────────────────────────────────────┐
│  📊 Stock Selector                  │
│─────────────────────────────────────│
│                                     │
│  搜索: [_______________] 🔍        │
│                                     │
│  ☑ AAPL   Apple Inc.        $185  │
│  ☑ GOOGL  Alphabet Inc.     $142  │
│  ☐ MSFT   Microsoft Corp.   $378  │
│  ☐ TSLA   Tesla Inc.        $248  │
│                                     │
│  已选: 2 只                         │
│                                     │
├─────────────────────────────────────┤
│ 输入: (无)                          │
│ 输出: ○ stocks [Stock[]]           │
└─────────────────────────────────────┘
```

**属性面板配置:**
```yaml
节点属性:
  - 搜索关键词: string
  - 选中股票: Stock[]
  - 最大选择数: number (1-20)
  
输出数据:
  - stocks: Stock[]
    - symbol: string
    - name: string
    - price: number
```

#### 3.3.2 Technical Analysis (技术分析节点)

```
┌─────────────────────────────────────┐
│  ⚙️ Technical Analysis              │
│─────────────────────────────────────│
│                                     │
│  指标选择:                          │
│  ☑ MACD   ☑ RSI    ☑ 布林带       │
│  ☐ KDJ    ☑ 均线   ☐ 成交量       │
│                                     │
│  时间周期: [日线 ▼]                 │
│  回溯天数: [90]                     │
│                                     │
├─────────────────────────────────────┤
│ 输入: ● stocks [Stock[]]           │
│ 输出: ○ analysis [Analysis]        │
└─────────────────────────────────────┘
```

#### 3.3.3 Strategy Generator (策略生成器)

```
┌─────────────────────────────────────┐
│  📋 Strategy Generator              │
│─────────────────────────────────────│
│                                     │
│  投资期限: [中期 (1-3月) ▼]        │
│  风险偏好: [稳健型 ▼]              │
│                                     │
│  ☑ 生成投资论点                    │
│  ☑ 生成技术分析                    │
│  ☑ 生成风险评估                    │
│                                     │
├─────────────────────────────────────┤
│ 输入: ● analysis [Analysis[]]      │
│       ● context [String] (可选)    │
│ 输出: ○ strategy [Strategy]        │
└─────────────────────────────────────┘
```

#### 3.3.4 Plan Generator (计划生成器)

```
┌─────────────────────────────────────┐
│  ⚡ Plan Generator                   │
│─────────────────────────────────────│
│                                     │
│  持仓状态: ○ 无持仓  ● 有持仓      │
│  ├── 成本价: [$178.50]             │
│  └── 仓位比例: [15%]               │
│                                     │
│  投资期限: [中期 ▼]                 │
│  风险等级: [════●═══] 60           │
│                                     │
├─────────────────────────────────────┤
│ 输入: ● strategy [Strategy]        │
│ 输出: ○ plan [ExecutionPlan]       │
└─────────────────────────────────────┘
```

#### 3.3.5 Condition Switch (条件分支)

```
┌─────────────────────────────────────┐
│  🔧 Condition Switch                │
│─────────────────────────────────────│
│                                     │
│  条件: [strategy.grade ▼]          │
│                                     │
│  规则:                              │
│  ├── grade == 'A' → 输出 1         │
│  ├── grade == 'B' → 输出 2         │
│  └── else         → 输出 3         │
│                                     │
├─────────────────────────────────────┤
│ 输入: ● data [Any]                 │
│ 输出: ○ output1 [Any]              │
│       ○ output2 [Any]              │
│       ○ output3 [Any]              │
└─────────────────────────────────────┘
```

---

## 四、交互设计

### 4.1 节点操作

```
┌─────────────────────────────────────────────────────────────────┐
│                        节点交互方式                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🖱️ 拖拽添加                                                    │
│     从左侧节点面板拖拽节点到画布                                │
│                                                                 │
│  🔗 建立连接                                                    │
│     从输出端口拖拽到另一节点的输入端口                          │
│     - 类型兼容检查 (自动)                                       │
│     - 连接线实时跟随鼠标                                        │
│     - 不兼容端口显示红色高亮                                    │
│                                                                 │
│  ⌨️ 快捷键                                                      │
│     Delete / Backspace - 删除选中节点/连接                      │
│     Ctrl+C / Ctrl+V    - 复制/粘贴节点                          │
│     Ctrl+Z / Ctrl+Y    - 撤销/重做                              │
│     Ctrl+A             - 全选                                   │
│     Space + 拖拽       - 平移画布                               │
│     滚轮               - 缩放画布                               │
│     F                  - 适应窗口 (Fit View)                    │
│                                                                 │
│  📋 右键菜单                                                    │
│     节点上: 复制 / 删除 / 禁用 / 查看文档                       │
│     画布上: 粘贴 / 添加注释 / 对齐节点 / 清空画布               │
│     连接线: 删除连接                                            │
│                                                                 │
│  🔍 节点搜索                                                    │
│     Ctrl+K 打开搜索框，快速添加节点                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 画布操作

```
┌─────────────────────────────────────────────────────────────────┐
│                        画布功能                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔭 视图控制                                                    │
│     - 缩放: 10% ~ 200%                                         │
│     - 小地图 (Mini Map): 右下角显示全局预览                     │
│     - 网格线: 可选显示，辅助对齐                                │
│                                                                 │
│  📐 自动布局                                                    │
│     - 水平布局 (左→右)                                         │
│     - 垂直布局 (上→下)                                         │
│     - 自动排列选中节点                                          │
│                                                                 │
│  🎨 视觉反馈                                                    │
│     - 选中节点: 蓝色边框 + 阴影                                 │
│     - 悬停节点: 边框高亮                                        │
│     - 运行中节点: 脉冲动画                                      │
│     - 完成节点: 绿色勾号                                        │
│     - 错误节点: 红色警告                                        │
│                                                                 │
│  🔀 连接线样式                                                  │
│     - 默认: 贝塞尔曲线                                          │
│     - 可选: 直线 / 折线                                         │
│     - 运行时: 流动动画                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 运行模式

```
┌─────────────────────────────────────────────────────────────────┐
│                        运行模式                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ▶️ 完整运行                                                    │
│     从 Start 节点开始，执行整个工作流                           │
│                                                                 │
│  ⏯️ 逐步执行 (Debug Mode)                                       │
│     单步执行，每个节点暂停，可查看中间数据                      │
│                                                                 │
│  🎯 从此节点运行                                                │
│     右键节点 → "从此节点运行"                                   │
│     使用缓存的上游数据                                          │
│                                                                 │
│  🔄 热重载                                                      │
│     修改节点参数后，自动重新执行受影响的节点                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、数据流设计

### 5.1 典型工作流示例

**示例 1: 基础策略生成**

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Start   │───▶│  Stock   │───▶│   AI     │───▶│ Strategy │
│          │    │ Selector │    │ Analysis │    │ Generator│
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                     │
     ┌───────────────────────────────────────────────┘
     │
     ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│  Plan    │───▶│  Save    │───▶│   End    │
│ Generator│    │  Output  │    │          │
└──────────┘    └──────────┘    └──────────┘
```

**示例 2: 多框架组合分析**

```
                              ┌──────────────┐
                         ┌───▶│  Technical   │───┐
                         │    │  Analysis    │   │
┌────────┐  ┌────────┐   │    └──────────────┘   │    ┌──────────────┐
│ Start  │─▶│ Stock  │───┤                       ├───▶│   Strategy   │
│        │  │Selector│   │    ┌──────────────┐   │    │   Merger     │
└────────┘  └────────┘   ├───▶│ Fundamental  │───┤    └──────┬───────┘
                         │    │  Analysis    │   │           │
                         │    └──────────────┘   │           ▼
                         │                       │    ┌──────────────┐
                         │    ┌──────────────┐   │    │   Strategy   │
                         └───▶│  Sentiment   │───┘    │   Generator  │
                              │  Analysis    │        └──────────────┘
                              └──────────────┘
```

**示例 3: 带条件分支的工作流**

```
┌────────┐   ┌────────┐   ┌────────┐   ┌──────────────┐
│ Start  │──▶│ Stock  │──▶│   AI   │──▶│   Strategy   │
│        │   │Selector│   │Analysis│   │  Generator   │
└────────┘   └────────┘   └────────┘   └──────┬───────┘
                                              │
                                              ▼
                                    ┌─────────────────┐
                                    │    Condition    │
                                    │    (Grade?)     │
                                    └────┬───────┬────┘
                                         │       │
                          grade A/B      │       │  grade C/N/A
                                         ▼       ▼
                              ┌──────────┐   ┌──────────┐
                              │   Plan   │   │  Notify  │
                              │Generator │   │ (警告)   │
                              └────┬─────┘   └────┬─────┘
                                   │              │
                                   ▼              ▼
                              ┌──────────┐   ┌──────────┐
                              │ Execute  │   │   End    │
                              │  Trade   │   │          │
                              └──────────┘   └──────────┘
```

### 5.2 数据传递格式

```typescript
// 节点间传递的数据包
interface NodeOutput {
  nodeId: string
  portId: string
  data: any
  timestamp: number
  metadata: {
    executionTime: number
    cached: boolean
  }
}

// 示例: Stock Selector 输出
{
  nodeId: "node_stock_1",
  portId: "stocks",
  data: [
    { symbol: "AAPL", name: "Apple Inc.", price: 185.50 },
    { symbol: "GOOGL", name: "Alphabet Inc.", price: 142.30 }
  ],
  timestamp: 1705760000000,
  metadata: {
    executionTime: 120,
    cached: false
  }
}

// 示例: Strategy Generator 输出
{
  nodeId: "node_strategy_1",
  portId: "strategy",
  data: {
    symbol: "AAPL",
    title: "AAPL 中期做多策略",
    direction: "LONG",
    grade: "A",
    thesis: "...",
    technicalAnalysis: "...",
    risks: "..."
  },
  timestamp: 1705760120000,
  metadata: {
    executionTime: 15000,
    cached: false
  }
}
```

---

## 六、工作流管理

### 6.1 工作流保存格式

```typescript
interface Workflow {
  id: string
  name: string
  description: string
  version: string
  createdAt: string
  updatedAt: string
  author: {
    id: string
    name: string
  }
  
  // 画布配置
  canvas: {
    zoom: number
    position: { x: number, y: number }
  }
  
  // 节点列表
  nodes: WorkflowNode[]
  
  // 连接列表
  edges: Edge[]
  
  // 变量定义
  variables: Variable[]
  
  // 元数据
  metadata: {
    tags: string[]
    isPublic: boolean
    usageCount: number
    rating: number
  }
}
```

### 6.2 预置模板

```
┌─────────────────────────────────────────────────────────────────┐
│                      预置工作流模板                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🚀 快速入门                                                    │
│  ├── 基础策略生成 (Basic Strategy)                             │
│  ├── 快速技术分析 (Quick Technical)                            │
│  └── 一键交易 (Quick Trade)                                    │
│                                                                 │
│  📊 分析专项                                                    │
│  ├── 技术分析专家 (Technical Expert)                           │
│  ├── 基本面深度分析 (Deep Fundamental)                         │
│  ├── 市场情绪分析 (Sentiment Analyzer)                         │
│  └── 多因子综合分析 (Multi-Factor)                             │
│                                                                 │
│  🎯 策略类型                                                    │
│  ├── 短期波段策略 (Swing Trading)                              │
│  ├── 中期价值投资 (Value Investing)                            │
│  ├── 事件驱动策略 (Event Driven)                               │
│  └── 动量策略 (Momentum)                                       │
│                                                                 │
│  ⚡ 自动化                                                      │
│  ├── 定时扫描策略 (Scheduled Scan)                             │
│  ├── 条件触发交易 (Conditional Trade)                          │
│  └── 批量处理流程 (Batch Processing)                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 导入导出

```
支持格式:
├── .workflow.json  - 原生 JSON 格式
├── .workflow.yaml  - YAML 格式 (可读性更好)
└── 剪贴板          - 复制粘贴分享

导出选项:
├── 包含凭证      ☐  (API Key 等敏感信息)
├── 包含缓存数据  ☐
└── 压缩格式      ☑
```

---

## 七、技术实现方案

### 7.1 技术选型

| 层级 | 技术方案 | 备选方案 |
|------|---------|---------|
| **框架** | Vue 3 + Composition API | React |
| **节点库** | Vue Flow | Rete.js, LiteGraph |
| **状态管理** | Pinia | Vuex |
| **样式** | Tailwind CSS + tokens | CSS Modules |
| **拖拽** | @vueuse/core (useDraggable) | vue-draggable |
| **存储** | LocalStorage + IndexedDB | |
| **后端通信** | Axios + WebSocket | fetch |

### 7.2 Vue Flow 集成示例

```vue
<template>
  <div class="workflow-editor h-screen flex">
    <!-- 节点面板 -->
    <NodePalette @drag-start="onDragStart" />
    
    <!-- 画布 -->
    <VueFlow
      v-model:nodes="nodes"
      v-model:edges="edges"
      :node-types="nodeTypes"
      :default-zoom="1"
      :min-zoom="0.1"
      :max-zoom="2"
      fit-view-on-init
      @node-click="onNodeClick"
      @connect="onConnect"
      @drop="onDrop"
      @dragover="onDragOver"
    >
      <Background />
      <MiniMap />
      <Controls />
    </VueFlow>
    
    <!-- 属性面板 -->
    <PropertiesPanel 
      :selected-node="selectedNode"
      @update="onNodeUpdate"
    />
    
    <!-- 底部控制栏 -->
    <ExecutionBar 
      :status="executionStatus"
      @run="runWorkflow"
      @stop="stopWorkflow"
    />
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'
import { VueFlow, useVueFlow } from '@vue-flow/core'
import { Background, MiniMap, Controls } from '@vue-flow/additional-components'

// 自定义节点类型
const nodeTypes = {
  stockSelector: StockSelectorNode,
  technicalAnalysis: TechnicalAnalysisNode,
  strategyGenerator: StrategyGeneratorNode,
  planGenerator: PlanGeneratorNode,
  conditionSwitch: ConditionSwitchNode,
  // ... 更多节点
}

const nodes = ref([])
const edges = ref([])
const selectedNode = ref(null)

// 执行工作流
async function runWorkflow() {
  const { getNodes, getEdges } = useVueFlow()
  const executor = new WorkflowExecutor(getNodes(), getEdges())
  await executor.run()
}
</script>
```

### 7.3 目录结构

```
src/
├── pages/
│   └── WorkflowEditor/
│       ├── index.vue              # 主页面
│       ├── components/
│       │   ├── NodePalette.vue    # 节点面板
│       │   ├── PropertiesPanel.vue # 属性面板
│       │   ├── ExecutionBar.vue   # 执行控制栏
│       │   └── MiniMap.vue        # 小地图
│       ├── nodes/                 # 节点组件
│       │   ├── BaseNode.vue       # 基础节点
│       │   ├── StockSelectorNode.vue
│       │   ├── TechnicalAnalysisNode.vue
│       │   ├── StrategyGeneratorNode.vue
│       │   ├── PlanGeneratorNode.vue
│       │   ├── ConditionSwitchNode.vue
│       │   └── ...
│       ├── composables/           # 组合式函数
│       │   ├── useWorkflow.js     # 工作流管理
│       │   ├── useExecution.js    # 执行引擎
│       │   ├── useNodeRegistry.js # 节点注册
│       │   └── useHistory.js      # 历史记录 (撤销/重做)
│       └── utils/
│           ├── executor.js        # 工作流执行器
│           ├── validator.js       # 验证器
│           └── serializer.js      # 序列化/反序列化
├── stores/
│   └── workflow.js                # Pinia store
└── types/
    └── workflow.d.ts              # 类型定义
```

---

## 八、MVP 开发计划

### Phase 1: 基础框架 (2周)

```
Week 1:
├── 搭建页面框架和布局
├── 集成 Vue Flow
├── 实现基础节点渲染
└── 实现节点拖拽添加

Week 2:
├── 实现节点连接
├── 实现属性面板
├── 实现工作流保存/加载
└── 基础样式完善
```

### Phase 2: 核心节点 (2周)

```
Week 3:
├── Stock Selector 节点
├── AI Analysis 节点 (复用现有逻辑)
├── Strategy Generator 节点
└── 节点间数据传递

Week 4:
├── Plan Generator 节点
├── Condition Switch 节点
├── 工作流执行引擎 (串行)
└── 运行状态可视化
```

### Phase 3: 增强功能 (2周)

```
Week 5:
├── 更多分析节点 (Technical, Fundamental, etc.)
├── 执行节点 (Paper Trade, Broker)
├── 并行执行支持
└── 错误处理和重试

Week 6:
├── 预置模板
├── 导入/导出
├── 快捷键支持
└── 性能优化
```

---

## 九、待讨论问题

### 9.1 设计决策

1. **节点粒度**: 是细粒度 (每个指标一个节点) 还是粗粒度 (按功能分组)?
2. **执行模式**: 是否需要后端执行? 还是全部前端执行?
3. **实时预览**: 是否在编辑时实时显示数据预览?
4. **版本控制**: 是否需要工作流版本管理?

### 9.2 扩展方向

1. **协作编辑**: 多人实时编辑同一工作流
2. **市场**: 工作流模板市场，用户可分享/购买
3. **调度**: 定时执行工作流
4. **API**: 提供 API 调用工作流

---

## 十、参考资源

- [Vue Flow](https://vueflow.dev/) - Vue 节点编辑器库
- [Rete.js](https://rete.js.org/) - 节点编辑器框架
- [n8n](https://n8n.io/) - 工作流自动化平台
- [Node-RED](https://nodered.org/) - 流程编程工具
- [ComfyUI](https://github.com/comfyanonymous/ComfyUI) - AI 图像生成工作流

---

*文档版本: Draft v0.1*  
*编写日期: 2026-01-20*
